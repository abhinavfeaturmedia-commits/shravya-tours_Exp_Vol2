# Execution

Deterministic scripts that handle the actual work. These scripts:

- Make API calls, process data, interact with databases
- Are reliable, testable, and fast
- Use environment variables from `.env` for configuration
- Are well-commented for maintainability

**Rule:** Always check here before writing a new script — an existing one may already do what you need.
